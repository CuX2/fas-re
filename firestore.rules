rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /restock-reports/{document=**} {
      allow read, write: if request.auth != null;
    }
    match /stores/{document} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}